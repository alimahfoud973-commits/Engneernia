# حالة المشروع — نقطة الاستمرارية بين الجلسات

> **اقرأ هذا الملف أولاً في كل جلسة، وحدّثه في آخرها.** يصف الحالة الفعلية الآن،
> لا الخطة. ما لم يُتحقَّق منه موسوم بذلك صراحة.
> القواعد الدائمة في `CLAUDE.md`، والقرارات التجارية في `docs/DECISIONS.md`.

**آخر تحديث:** 2026-09-26. آخر commit للكود: `ac8173c`؛ ما بعده على `main`
commits توثيق فقط (هذا الملف و`CLAUDE.md`). تحقّق بـ `git log --oneline -5`.

---

## ١. الهدف الحالي

**Enginora / إنجينورا**: متجر عربي للموارد الهندسية الرقمية، بمالك واحد وأربعة
تخصصات، وعمولة لكل مهندس، وتسوية شهرية. الهدف الآن **نسخة نهائية مرشّحة
للاستضافة**: كل خلل حقيقي يُصلَح ويدخل `main`، ولا ميزات جديدة.

## ٢. الوضع الحالي

| البند | الحالة |
|---|---|
| الفرع | `main` = `origin/main`؛ آخر commit للكود `ac8173c`، تليه commits توثيق فقط |
| الـ working tree | نظيف |
| GitHub CI | **أخضر** على `ac8173c` (التشغيل رقم 12)، وكذلك منذ `75459bb` |
| اختبارات الوحدة | **721/721** (`npm run verify`: أنواع + lint + وحدة + بناء) |
| اختبارات التكامل | **494/494** على تخزين S3 (RustFS) وعلى `file://` |
| آخر ترحيل | `0056_register_without_email_confirmation.sql` |
| فرع العمل القديم | `claude/vigilant-cerf-g5xmlq` ما زال على `57bf7da` (متأخر عن `main`) |
| الفرع الافتراضي في GitHub | `_probe_write_access` (ليس `main`) — لم يُغيَّر |

## ٣. ما أُنجز في دورة التدقيق الحالية (مدموج في `main`)

الترتيب زمني. كل بند مرّ بدورة: اكتشاف ← إصلاح ← اختبار ← قبول المالك ← إغلاق.

| commit | البند |
|---|---|
| `1f391eb` | **F1** — المنتج المجاني يُحصَل عليه دون دفعة (ترحيل 0054) |
| `cd019f8` `e29cf94` `5a8ca2c` | **F2** — لا نشر ولا بيع لمنتج مدفوع بلا اتفاق عمولة سارٍ |
| `941f98a` | **F3** — شاشة طرق الدفع للمالك + لقطة بيانات الطريقة على الدفعة (ترحيل 0055) |
| `83a5a6c` | **W2** — رقم واتساب يضبطه المالك (`/admin/settings`) ومساعدة واتساب تعمل |
| `57bf7da` | **الدخول** — لا تأكيد بريد: الحساب الجديد `ACTIVE` ويدخل مباشرة (ترحيل 0056) |
| `9ddf62e` | (من المالك) إضافة `@swc/core-win32-x64-msvc` لـ Windows |
| `caa1634` | **F4** — `readTaxPolicy` يقرأ القيم النصية من JSONB صحيحاً (الرقم الضريبي والبادئة) |
| `5d9a306` | **Linux/SWC** — الحزمة صارت `optionalDependencies` فعاد `npm ci` يعمل على Linux وDocker |
| `75459bb` | **CI** — خدمة RustFS في GitHub Actions؛ اختبارات التكامل تعمل على S3 حقيقي |
| `86d5227` | **Docker المحلي** — RustFS بدل MinIO في `docker-compose.yml` (صور MinIO لم تعد تُسحب) |
| `ac8173c` | **Preview Display** — معاينة PDF تظهر داخل صفحة المنتج |

## ٤. قرارات يجب عدم مخالفتها

- **الدخول**: بالبريد + كلمة المرور فقط، ودخول مباشر بعد كلمة المرور الصحيحة.
  لا تأكيد بريد ولا رمز OTP بالبريد. رسالة التسجيل موحّدة (لا تكشف وجود البريد)،
  ولا بريد يُرسل عند التسجيل. عنوان مسجّل لا يُعدَّل بإعادة التسجيل (لا استيلاء).
- **TOTP للمالك يبقى إلزامياً للوحة الإدارة** (`requireOwner`). إلغاؤه موضوع منفصل
  لم يُقرَّر. TOTP للمستخدمين الآخرين فقط لمن فعّله.
- **الدخول باسم المستخدم أو الهاتف = المرحلة 6**، مؤجّلة. لا أعمدة لها في القاعدة.
- **المنتج المجاني** يكتمل فوراً بلا دفعة (`app_complete_free_order`).
- **لا بيع بلا اتفاق عمولة سارٍ** للمنتج المدفوع، ولا نسبة عمولة افتراضية في الكود.
- **بيانات طريقة الدفع تُنسخ على الدفعة** عند إنشائها؛ تعديل الطريقة لا يمس طلباً قائماً.
- **رقم واتساب يُقرأ من الإعدادات لحظة بناء الرابط**، ولا يُنسخ على الطلب.
- **المعاينة تُقدَّم من أصل الموقع نفسه دائماً** (تُقرأ من التخزين وتُبثّ، لا تحويل
  إلى رابط موقّع). وحدها قابلة للتضمين، ومن الموقع نفسه فقط
  (`frame-ancestors 'self'` + `X-Frame-Options: SAMEORIGIN`). كل مسار آخر
  `X-Frame-Options: DENY`، و CSP الصفحة `frame-src 'self'` لا تسمّي أي مضيف تخزين.
  يحرس ذلك `src/lib/security/framing.test.ts`.
- **الملف الأصلي**: رابط موقّع 60 ثانية بعد التحقق من الملكية، و`attachment`.
  لا bucket عام.
- **التخزين**: `file://` هو الافتراضي المحلي. خدمة S3 المحلية وCI هي
  `rustfs/rustfs:1.0.0` (مثبّتة)، وتهيئة الـ buckets بـ `amazon/aws-cli:2.37.4`.
  لا MinIO: صوره أُزيلت من Docker Hub وquay.io يرد 401 (تحقّق المالك على جهازه).
- **`src/finance/policy.ts` تُرك عمداً** دون تعديل في F4 (قيمه الواقعية تُقرأ صحيحاً).
- **سلوك `tax.rateBp` لم يتغيّر** في F4، ومثبّت باختبار.

## ٥. قواعد العمل التي حدّدها المالك

- **اكتشاف ← إصلاح ← اختبار ← قبول ← إغلاق.** تدقيق أولاً، ولا تعديل قبل دليل.
- **مشكلة مستقلة تظهر أثناء بند: تُسجَّل ولا تُصلَح** ضمنه.
- **لا ميزات جديدة.** لا تحسينات تجميلية تُقدَّم على أنها أخطاء.
- **commit/push فقط بإذن المالك لكل بند.** العمل على `main`، مع `git diff --check`
  و`npm run verify` قبل الـ commit، والتأكد بعد الـ push أن `main = origin/main`
  وأن CI أخضر.
- **ممنوع**: force push، إعادة كتابة التاريخ، حذف فروع، التراجع عن إصلاحات سابقة.
- **لا تُمَس** `package.json` و`package-lock.json` إلا حين يكون البند نفسه عنهما
  وبموافقة (كما في إصلاح SWC). **`CLAUDE.md` لا يُعدَّل إلا بطلب صريح من المالك.**
- **لا إضعاف لـ RLS**، ولا دالة `SECURITY DEFINER` جديدة إلا بضرورة مُثبتة.
- **لا ترحيل** إلا بضرورة، ويُذكر السبب للمالك قبل التنفيذ.
- **التجارب على قواعد بيانات مؤقتة** تُحذف بعدها، لا على قاعدة المالك.

## ٦. المرحلة الحالية والخطوة التالية

**المرحلة:** تدقيق ما قبل الاستضافة (امتداد تدقيق Stage 2 لرحلة المشتري).
أُغلقت F1–F4 وW2 وبنود البنية (SWC، CI، Docker، المعاينة).

**الخطوة التالية — تنتظر قرار المالك** (لم يُحدَّد الترتيب بعد):

1. **البند المستقل `src/media/ingest.ts:161`** — طلب المالك تسجيله بنداً مستقلاً
   بعد إغلاق MinIO. صف المعاينة يُكتب بـ `byteSize: BigInt(1)` و`sha256: ''`
   كقيم ثابتة، مع أن الملف الحقيقي له حجم وبصمة (مثال مُقاس: 569,520 بايت).
2. **D1 ثم D3 ثم D4** من تدقيق Stage 2 (انظر §٧). قال المالك صراحة: لا انتقال
   إليها قبل إغلاق البند الجاري.

## ٧. المشاكل المفتوحة

| # | المشكلة | الحالة |
|---|---|---|
| 1 | `ingest.ts:161`: `byteSize: 1` و`sha256: ''` لصف المعاينة | مُثبتة، مسجّلة، لم تُصلَح |
| D1 | لا `not-found.tsx` ولا `error.tsx`: صفحة 404 والخطأ من Next بالإنجليزية | مُثبتة (لا ملفات)، لم تُصلَح |
| D3 | الترويسة تكتب التخصصات الأربعة يدوياً (`src/components/site-chrome.tsx:9-12`) | مُثبتة، لم تُصلَح |
| D4 | عنوان تبويب صفحات 404 يبقى باسم المنصة (شكلي) | مسجّلة من Stage 2، لم يُعَد التحقق |
| W1 | رابط «أنشئ حساباً» في `/login` بلا `next`، فيصل المشتري إلى `/account` لا إلى المنتج | مُثبتة (السطر 59)، صُنِّفت «ليست Bug» |
| — | `THUMBNAIL` لو استُخدم في `<img>` مع S3 سيُحوَّل إلى مضيف التخزين ويمنعه `img-src 'self'` | ملاحظة؛ لا واجهة تستخدمه اليوم |
| — | إعادة المحاولة بعد رفض إيصال الدفع تعيد استخدام صف الدفعة القديم | سُجّلت في W2 خارج النطاق؛ **لم يُعَد التحقق منها** |
| — | `docs/DECISIONS.md` (OPEN-23، §١٠) ما زال يصف «تسجيل ذاتي مع تأكيد البريد» | الوثيقة متأخرة عن ترحيل 0056 |
| — | `docs/PREVIEW-TEST-REPORT.md` يذكر MinIO | تقرير تاريخي، لم يُعدَّل |
| — | الفرع الافتراضي في GitHub `_probe_write_access` | لم يُغيَّر |

**ليست أخطاء في التطبيق (ملاحظات تشغيل):**
- تشغيل Next مقيّداً بـ `-H 127.0.0.1` وحده يُدخل الصفحات في حلقة تحويل (307 إلى
  نفسها)، لأن إعادة الكتابة الداخلية تستهدف `localhost`. الربط الافتراضي و
  `-H 0.0.0.0` يعملان. Docker يستعمل الافتراضي.
- على جهاز المالك: الـ volume القديم `minio_data` لم يعد مستخدماً (الجديد `storage_data`).

## ٨. معلومات تقنية للمتابعة

**التشغيل المحلي**
```bash
docker compose up -d            # PostgreSQL + RustFS + تهيئة الـ buckets
docker compose up -d postgres   # Postgres وحده (التخزين الافتراضي file://)
npm run db:migrate              # بعد كل pull فيه ترحيل جديد
npm run verify                  # قبل أي commit
npm run test:integration        # يحتاج قاعدة حقيقية
```
- لاستعمال RustFS بدل القرص: `STORAGE_ENDPOINT=http://localhost:9000` في `.env.local`.
  المفاتيح `minioadmin` وأسماء الـ buckets كما في `.env.example`.

**CI** (`.github/workflows/ci.yml`): خدمتا `postgres:16-alpine` و`rustfs/rustfs:1.0.0`،
ثم npm ci ← typecheck ← lint ← وحدة ← إثبات RLS ← ترحيلات ← seeds
(`seed:catalog` `seed:demo` `seed:scale`) ← إنشاء الـ buckets ← تكامل ← بناء ← `npm audit --audit-level=high`.

**قاعدة مؤقتة للتجارب** (كما يفعل CI): أنشئ القاعدة، امنح `migrator` كل الصلاحيات
و`app_user` الاتصال، واجعل `migrator` مالك `public` مع
`ALTER DEFAULT PRIVILEGES FOR ROLE migrator ... TO app_user` (انظر
`docker/postgres/init/01-roles.sql`)، ثم `db:migrate` والـ seeds، واحذفها بعد الانتهاء.
`seed:demo-media` يحتاج صف مالك موجوداً.

**مزالق مُثبتة**
- `next dev` يُلحق كتلة قواعد بـ `CLAUDE.md` حين يكتشف أنه يعمل داخل وكيل ذكي؛
  شغّله بـ `env -i PATH="$PATH" HOME="$HOME" npx next dev` لتفاديه.
- Drizzle يحلّل عمود `jsonb` مرتين: نص مثل `"2026"` يعود رقماً. اقرأ الإعدادات
  النصية بـ `${settings.value}::text` ثم `JSON.parse` (كما في `settings.ts`
  و`tax-policy.ts`).
- header في `next.config.ts` يتغلّب على header يضعه المسار لنفس المفتاح.
- `npm` يرفض بـ `EBADPLATFORM` أي تبعية مباشرة غير اختيارية لمنصة أخرى.

**بيئة Claude Code السحابية** (خاص بها، مُتحقَّق منه):
- Postgres المحلي: `bash scripts/dev-db.sh` (قد يتوقف عند إعادة تشغيل الحاوية).
- Docker: يلزم تشغيل `dockerd` يدوياً؛ quay.io محجوب بسياسة الشبكة؛ Docker Hub
  قد يرد 429.
- `gh` غير متاح؛ حالة CI تُقرأ من واجهة GitHub العامة أو أدوات MCP.
