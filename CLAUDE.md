# Engineering Marketplace — Project Rules

منصة موارد هندسية رقمية يديرها مالك واحد، بأربعة تخصصات، وعمولة ديناميكية،
وتسوية شهرية للمهندسين.

**المصادر المرجعية — اقرأها قبل أي تغيير جوهري:**
- `docs/PROJECT_SPECIFICATION.md` — المواصفات الأساسية (§1–53)
- `docs/ADDITIONAL_DECISIONS.md` — قرارات المالك المكمّلة
- `docs/DECISIONS.md` — سجل القرارات المعتمدة والمعلّقة
- `docs/ROADMAP.md` — المراحل ومعايير الخروج من كل مرحلة

---

## قواعد غير قابلة للتفاوض

هذه ليست تفضيلات أسلوبية. كسر أي واحدة منها يعني عيباً في المنتج.

1. **لا تُغيَّر السجلات المالية التاريخية.** تغيير السعر أو العمولة لا يمس أي عملية بيع سابقة. اللقطة المالية تُكتب مرة واحدة وتحميها قيود قاعدة البيانات.
2. **لا تُحسب المبالغ بأعداد عشرية.** كل مبلغ `bigint` بوحدات صغرى مع عملة إلزامية. النسب basis points صحيحة. استخدم `src/lib/money` ولا تكتب حساباً مالياً خارجه.
3. **يُقرَّب طرف واحد فقط من القسمة**، والآخر هو الباقي — دائماً.
4. **الصلاحيات تُفرض على الخادم.** أربع طبقات: بوابة المسار، دالة السياسة، RLS في PostgreSQL، ثم DTO مخصص لكل فئة مشاهد. إخفاء البيانات في الواجهة ليس حماية.
5. **الوصول غير المصرّح يعيد `404` لا `403`.** إخبار المستخدم أن المورد موجود لكنه ليس له = ثغرة تعداد.
6. **لا يُعاد كيان النطاق (entity) من مسار أبداً.** يُحوَّل عبر `toPublic` / `toContributor` / `toOwner`. الحقول المالية تكون **غائبة** لا مخفية.
7. **الملف الأصلي لا يملك رابطاً عاماً.** لا يوجد bucket عام في المشروع. التنزيل عبر تحقق من الملكية ثم رابط موقّع قصير الأجل.
8. **الإشعار له مستقبل واحد.** `notifications.user_id` إلزامي. لا توجد واجهة تقبل قائمة مستقبلين.
9. **لا مبلغ عمولة ثابت في الكود.** أبداً. حتى كقيمة افتراضية مؤقتة.
10. **لا دفعة للمهندس بعد كل عملية بيع.** التسوية شهرية فقط.
11. **لا ربط بمزوّد دفع واحد.** أي مزوّد جديد = محوّل جديد + صف في قاعدة البيانات، لا تعديل في المحاسبة.
12. **كل إجراء إداري أو مالي حساس يُسجَّل في سجل التدقيق** داخل نفس المعاملة.

## قاعدة عدم التخمين

عند مواجهة قرار تجاري غير محسوم (انظر جدول OPEN في `docs/DECISIONS.md`):
**لا تخمّن.** اعرض الخيارات واطلب قرار المالك. إن كان الكود لا يستطيع الانتظار،
اجعله **يرفض العملية بخطأ واضح** يذكر رقم OPEN — كما في الخصومات (OPEN-1) —
بدل أن يختار سلوكاً مالياً بصمت.

## طريقة العمل

- مرحلة واحدة في كل مرة، مع اختبارات، ثم توقّف للمراجعة.
- قبل أي تغيير مدمّر: اشرح ما سيتغير، وما الملفات المتأثرة، وانتظر الموافقة.
- `npm run verify` يجب أن يمرّ قبل أي commit.

## أوامر

> **لا تُصدّر `.env.local` إلى الصدفة قبل `npm run build`.** كانت تحمل
> `NODE_ENV=development`، و`next build` يحترمها فيفشل التصيير المسبق برسالة
> React مبهمة. حُذف المتغيّر وأُضيف حارس `prebuild` يرفض البناء بقيمة غير
> `production` (KI-1 في `docs/KNOWN-ISSUES.md`). الأوامر التي تحتاج الاتصال
> بقاعدة البيانات (`db:migrate`، `test:integration`) تصدير الملف لها آمن.

```bash
npm run dev            # خادم التطوير
npm run verify         # فحص الأنواع + lint + اختبارات + بناء
npm run test:watch     # اختبارات تفاعلية
npm run db:migrate     # تطبيق الترحيلات
npm run db:prove-rls   # إثبات عزل الصلاحيات على قاعدة بيانات حقيقية
npm run test:integration  # اختبارات RLS والدخول على قاعدة حقيقية
npm run bootstrap:owner   # إنشاء حساب المالك (مرة واحدة)
node scripts/security-probe.mjs  # ٤٨ فحصاً أمنياً على بناء إنتاجي يعمل
node scripts/csp-check.mjs       # إثبات أن CSP لا تكسر الموقع في متصفح حقيقي
node scripts/measure-pages.mjs   # زمن كل صفحة وعدد استعلاماتها
npm run backup                   # نسخة احتياطية (أدوار + قاعدة + بيان)
npm run restore-drill            # تمرين استعادة كامل مع فحوص الهوية والسلامة
docker compose up -d   # PostgreSQL + MinIO محلياً
```

## الحالة

المراحل **P0 إلى P7 مكتملة**. التالي: **P8 — الإشعارات والتقييمات والأداء والنسخ الاحتياطي والمراجعة الأمنية والنشر**.

### قواعد المال (P5)

- **اللقطة المالية تُكتب مرة واحدة** عند `PAID` ويحرسها trigger في القاعدة.
  تغيير السعر أو العمولة بعدها لا يصلها — مُثبت باختبار.
- **المالك وحده يصل إلى `PAID`.** هذا الانتقال وحده يكتب اللقطة ويمنح الاستحقاق.
- **لا يوجد سعر عمولة افتراضي.** بيع بلا اتفاق سارٍ **يُرفض** بدل اختراع نسبة.
- **الاستحقاق صف في القاعدة** لا رسالة نجاح. مسار التنزيل يفحصه عبر RLS.
- **أسرار مزوّدي الدفع في جدول منفصل** لأن RLS يحمي الصفوف لا الأعمدة.

### حدود الاختبارات — عيوب لا يكشفها أي منها

ثلاثة عيوب شُحنت وكل الاختبارات خضراء، وكُشفت **بتشغيل الموقع والنظر إليه**:

- **ملف `'use server'` يُصدّر ثابتاً** → 500 وقت التشغيل، بلا خطأ أنواع ولا بناء
  فاشل. يحرسه `src/lib/server-actions.test.ts`.
- **مكوّن خادم يستورد قيمة من مكوّن `'use client'`** → يحصل على مرجع عميل لا
  على القيمة، فتظهر القيمة الخام بدل النص العربي. يحرسه `src/lib/labels.test.ts`،
  والعناوين كلها في `src/lib/labels.ts`.
- **`@theme` داخل `@media`** يُرفع فيُطبَّق دائماً. يحرسه `src/app/theme.test.ts`.
- **`getSql()` يُعيد الطوابع الزمنية نصوصاً لا `Date`** (P8). بناء عميل Drizzle
  يركّب محلّلات أنواع على اتصال postgres.js نفسه. النوع العام على الاستعلام
  الخام **دعوى لا فحص**، فـ`row.some_at.getTime()` يمرّ من المترجم ويرمي وقت
  التشغيل. عالِجها بـ`toDate(...)` كما في `src/auth/login.ts`، أو — الأفضل —
  اطلب الرقم النهائي من PostgreSQL كما في `src/lib/rate-limit.ts`.
- **مسار الخطأ نفسه لا يُختبَر** (P8). محدِّد المعدّل كان يحسب الحدّ صحيحاً
  ثم ينهار وهو يرفض. اختبار «يرفض» كان سينجح على الكود المعيب؛ ما كشفه هو
  اختبار يصل إلى **قيمة** الرفض (مدة الانتظار).

**القاعدة:** بعد أي عمل يمسّ واجهة، شغّل التطبيق فعلاً وافتح الصفحة. و**كل حارس
جديد يجب أن تتحقق أنه يفشل** بإعادة العيب قبل أن تعتمده — حارسٌ لا يفشل ليس حارساً.

### الأداء (P8)

- **أي قياس أداء يجب أن يُجرى بدور `app_user`.** superuser يتجاوز RLS، وقياسٌ
  به أظهر فرق ١٫٢ ms حيث الفرق الحقيقي ٣٣ ms — لأن تكلفة سياسات الصفوف سقطت من
  القياس كلياً.
- ترتيب «الأحدث أولاً» تعبير واحد: `newestFirst` في `public-queries.ts`،
  ويطابق الفهرس `products_recent_idx` حرفاً بحرف. تغيير أحدهما دون الآخر يعيد
  المسح الكامل بصمت.
- كل ترتيب يجب أن ينتهي بفاصل تعادل فريد (`id DESC`)، وإلا تداخلت الصفحات.
- `scripts/measure-pages.mjs` يقيس زمن الصفحة وعدد الاستعلامات من
  `pg_stat_statements`. القياسات والعتبات في `docs/PERFORMANCE.md`.

### الأمن (P8)

- ترويسة CSP تُبنى في `src/lib/security/csp.ts` وتُرسل من `src/proxy.ts` مع
  nonce لكل استجابة. **الـnonce يُوضع على الطلب أيضاً** وإلا لن يختم Next به
  نصوصه. لا تُضف `'unsafe-inline'` إلى `script-src` لتُصلح شيئاً.
- الوجهة بعد تسجيل الدخول تمرّ حصراً عبر `safeReturnPath` — `startsWith('/')`
  وحده يسمح بـ`//evil.com`.
- كل معرّف في مسار يُفحص بـ`isUuid` عند حافة الطلب: معرّف مشوَّه يجب أن يكون
  404 لا 500.
- بعد أي عمل يمسّ الصلاحيات أو الترويسات، شغّل `scripts/security-probe.mjs`
  على بناء إنتاجي. القائمة المرجعية الكاملة في `docs/SECURITY-REVIEW.md`.

### قرارات المالك المطبَّقة بعد P7

- **لا استرجاع في المنصة، لأي سبب.** البيع المكتمل نهائي. النظام **محذوف**
  لا معطَّل (الترحيل 0035)، و**دالة الترحيل في الدفتر ترفض `REFUND` و
  `REFUND_PAYOUT`** — فلا طريق لقيد استرجاع حتى من وحدة تحكّم بقاعدة البيانات.
  الحالة `COMPLETED` نهائية بلا مخرج. **لا تُعِد بناءه** دون قرار مالك جديد.
- **الدفعة المكررة لا تحتاج استرجاعاً**: لا يُعتمد الدفع الثاني، فلا بيع ولا
  استحقاق ولا إيراد.
- **`ADJUSTMENT` ليس استرجاعاً**: تصحيح يكتبه المالك بسبب مذكور، بلا عميل وبلا
  إعادة مال. له الآن شاشة `/admin/adjustments` (OPEN-21 محسوم):
  **معاينة ← تأكيد صريح**، والخطوة الثانية تُعيد التحقق من الصفر لأن الخلاصة
  التي رآها المالك ليست تصريحاً. الحسابان ثابتان ولا يختارهما مستخدم.
  الرصيد السالب **تحذير لا رفض**. السبب يُكتب على سطر الدفتر ليقرأه المهندس.
  جدول `financial_adjustments` **يُلحَق ولا يُعدَّل** — حتى المالك لا يعدّله.
- **الألوان أسود وذهبي وأبيض، ورموز فقط.** لا لون حرفي خارج `globals.css`،
  ولا `text-white` — استخدم `--color-accent-contrast`. وضع الليل يستخدم
  `:root:root` لأن `@theme` يُرفع خارج أي `@media`. يحرس ذلك `theme.test.ts`.


- **لا حد أدنى للصرف.** الإعداد `settlement.minimumPayoutMinor` صار `0`.
  **الآلية باقية** لأن قرار المالك في §8 كان ألّا يكون الرقم في الكود؛ إعادته
  صفٌّ في `settings` لا تعديل ونشر. والكشوف الصادرة تحتفظ بالحد وقت إصدارها.
- **البيع يُرسل إشعاراً، والشهر يُصدر مستنداً.** `PRODUCT_SOLD` لكل مساهم
  بحصته هو المجمَّدة وبلا أي هوية للمشتري (OPEN-4)، وكشف PDF شهري فقط.
- **كل صفحة عربية يولّدها الخادم تُرسَم على canvas ثم تُدرَج في PDF كصورة.**
  `pdf-lib` بلا تشكيل يُخرج حروفاً منفصلة معكوسة. استخدم
  `src/lib/typography/arabic.ts` ولا تكتب عربية عبر `pdf-lib` مباشرة أبداً.
- **الخط مضمَّن في `assets/fonts/`** ولا يُجلب وقت التشغيل.
- **أي مكتبة أصلية (`.node`) تُعلَن في `serverExternalPackages`** في
  `next.config.ts`، وإلا انكسر البناء عند أول Route Handler يصلها.

### قواعد التسوية (P7)

- **التسوية شهرية، ولا تُسوّى فترة لم تُغلق.** يبدأ التوليد في اليوم الأول من
  الشهر التالي بتوقيت دمشق (قرارات §8).
- **الكشف يشمل المُرحَّل، لا مبيعات الشهر وحدها.** المعادلة:
  `المستحق = ما تحقق حتى نهاية الفترة − ما استُرجع − ما صُرف فعلاً`.
  منها يتبع تلقائياً: الترحيل دون الحد الأدنى، والدين بعد استرجاع متأخر،
  وكون التوليد مرتين لا يصرف مرتين.
- **قطعان مختلفان عمداً**: التحقق والاسترجاع يُقتطعان **بالتاريخ**، والمصروف
  يُقتطع **بالفترة التي سُدّد عنها**. الخلط بينهما يجعل تسوية شهر قديم تخصم
  مدفوعات أشهر أحدث وتُظهر ديناً وهمياً.
- **`CARRIED_FORWARD` حالة نهائية.** كشف لم يُصرف لأن الرصيد دون الحد لا
  «ينتظر الاعتماد» — رصيده يدخل كشف الشهر التالي، واعتماده هنا يصرف الشهر مرتين.
- **الكشف المدفوع مجمَّد** بمُشغِّل في القاعدة، والملاحظة التشغيلية وحدها تبقى
  قابلة للكتابة. تصحيحه قيدُ تسوية جديد لا تعديل للكشف.
- **الدفتر هو المرجع، والتفاصيل وسيلة إيضاح.** إن تعذّر بناء التفاصيل يصدر
  الكشف بإجماليه الصحيح مع **سطر تسوية موسوم**، ولا يُحجب صرف مال مستحق.
- **الحد الأدنى يُجمَّد على الكشف** لحظة إصداره: تغيير الإعداد لاحقاً لا يعيد
  كتابة سبب عدم صرف شهر قديم.

### قواعد الدفتر (P6)

- **الدفتر هو الحقيقة المالية.** لا يوجد رصيد مخزّن في أي جدول. كل رقم مالي
  — رصيد مهندس، إيراد شهر، مستحق قائم — مجموعٌ فوق `ledger_lines` وقت السؤال.
- **المبلغ موقّع**: موجب مدين، سالب دائن. الثابت الوحيد: `SUM(amount_minor) = 0`.
- **الطريق الوحيد إلى الدفتر `app_post_ledger_transaction`.** دور التطبيق
  **لا يملك INSERT** على جداول الدفتر. لا تضف هذه الصلاحية أبداً — غيابها هو
  ما يجعل القيد غير المتوازن مستحيلاً لا مستبعداً.
- **الدفتر يُلحَق ولا يُعدَّل.** تصحيح الخطأ قيدٌ معاكس جديد، لا تعديل قيد قديم.
- **سلسلة التجزئة تكشف العبث المباشر.** `app_verify_ledger_chain()` تعيد أول
  قيد مكسور. أي تغيير في ترتيب البيانات المُجزَّأة يكسر كل السلسلة — لا تعدّل
  الصياغة القانونية (canonical payload) دون خطة إعادة تجزئة موثّقة.
- **لا رقم مالي يُعاد حسابه.** البيع يُرحَّل من اللقطة المجمّدة، والاسترجاع
  يعكس الأرقام نفسها بإشارة معكوسة. لا قسمة مرتين، فلا انحراف تقريب.
- **الاسترجاع لا يمسّ البيع.** يُعلَّم البند فقط (`refunded_at`)؛ أعمدة اللقطة
  يحرسها المُشغِّل كما كانت.
- **الشهر المحاسبي تحسبه القاعدة** بتوقيت دمشق. لا تمرّر `period_key` — يُهمَل.
- **لا رقم سياسة في الكود**: نافذة الاسترجاع والحد الأدنى للصرف صفوف في
  `settings` تُقرأ لحظة السؤال (قرارات المالك §7 و§8).

### قواعد الملفات (P3)

- **الأصل لا يملك رابطاً عاماً.** المسار الوحيد هو `/api/files/[slug]/[role]`،
  والتصريح يقرره RLS: صف الملف الأصلي **لا يُحل** لمن لا يملكه.
- **المعاينة لـPDF فقط** (قرار المالك). لا معاينة لـExcel أو DWG أو Revit أو المضغوطات.
- **المعاينة صور نقطية والعلامة المائية محروقة في البكسل** — لا طبقة نص، ولا أثر
  للصفحات المحجوبة في الملف أصلاً.
- **مفاتيح التخزين عشوائية** ولا تُشتق من العنوان أو اسم الملف أبداً.
- **الأرشيفات لا تُفك ضغطها** على الخادم، أبداً.
- كل تنزيل للأصل يُسجَّل **قبل** خروج البايتات.

### قاعدة الترحيلات

Drizzle لا يعرف الترحيلات المكتوبة يدوياً (RLS، الأعمدة المُولَّدة، القيود).
لذلك **كل `db:generate` بعد ترحيل يدوي يعيد اقتراح ما طُبِّق فعلاً** — راجع
الملف المُولَّد واحذف المكرر قبل التطبيق، وإلا أجهض `ALTER TYPE ... ADD VALUE`
الترحيل كاملاً.

**الترحيل المطبَّق لا يُعدَّل أبداً.** Drizzle يخزّن بصمة كل ملف في
`drizzle.__drizzle_migrations`؛ تعديل ملف بعد تطبيقه يترك كل قاعدة بيانات
طبّقته مختلفة بصمت عن الملف الذي يدّعي وصفها. التصحيح **ترحيل جديد** — كما في
0026 ← 0027. تاريخ الترحيلات يُلحَق ولا يُعدَّل، للسبب نفسه الذي يجعل الدفتر كذلك.

### ما يجب معرفته قبل كتابة أي كود يلمس قاعدة البيانات

- كل وصول يمرّ عبر `withActor(actor, tx => ...)` — يفتح معاملة ويعلن الفاعل لـRLS.
- سياق الفاعل **محلي للمعاملة** (`set_config(..., true)`) لأن الاتصالات مجمّعة.
- كل طابع زمني قادم من SQL يُقرأ عبر `toDate()` (انظر TD-11).
- أي إجراء جديد يُضاف إلى `src/authz/actions.ts` **يجب** أن يُضاف إلى جدول التوقعات
  في `src/authz/matrix.test.ts` — وإلا فشل البناء (النوع `Record<Action, ...>`).
- أي جدول جديد **يجب** أن يُفعَّل عليه RLS مع سياسات صريحة، وإلا كشفه اختبار «كل جدول عليه RLS».
- **RLS يرفض الكتابة بإرجاع صفر صفوف، لا برمي خطأ.** كل كتابة يجب أن تتحقق من
  `.returning()` وترمي إن لم تتأثر أي صفوف — وإلا بدت عملية مرفوضة وكأنها نجحت.
- `FORCE ROW LEVEL SECURITY` يطبَّق على مالك الجدول أيضاً، فيعطّل دوال
  `SECURITY DEFINER` الموثوقة. استخدمه فقط على جدول لا تقرأه أي دالة موثوقة.
  (تكرر هذا في `contributors` و`product_contributors` و`entitlements` وجداول الدفتر.)
- **الأثر المُلحَق فقط لا يحمل مفتاحاً أجنبياً.** جداول مثل `audit_logs`
  و`order_events` و`download_events` و`ledger_lines` تحمل معرّفات فقط، مع سياق
  مُكرَّر يجعلها مقروءة وحدها. المفتاح الأجنبي يجعل حذف حساب مستحيلاً إلى الأبد؛
  الصحة المرجعية تُفحص **وقت الكتابة** داخل الدالة الموثوقة بدلاً من ذلك.
- **كل دالة `SECURITY DEFINER` جديدة تثبّت `SET search_path = public, pg_temp`.**
  دالة تحلّ الأسماء عبر مسار المستدعي = وسيلة تصعيد صلاحيات.
- السعر لا يُعدَّل في مكانه أبداً: يُغلق الصف المفتوح ويُفتح صف جديد عبر
  `app_set_product_price` داخل معاملة واحدة.
